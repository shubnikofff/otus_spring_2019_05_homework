spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shubnikofff/config-repo/
          search-paths: "{application}"

server:
  port: 5001
