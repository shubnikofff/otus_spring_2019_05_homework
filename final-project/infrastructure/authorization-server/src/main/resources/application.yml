eureka:
  instance:
    instance-id: "${spring.application.name}:${server.port}"
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

feign:
  hystrix:
    enabled: true

application:
  oauth2:
    client-id: "open-shelf"
    client-secret: "book_to_book"
    signing-key: 987654321
