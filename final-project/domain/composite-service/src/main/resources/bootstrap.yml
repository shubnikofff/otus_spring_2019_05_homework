spring:
  application:
    name: composite-service
  cloud:
    config:
      uri: http://localhost:8888

encrypt:
  key: book-exchange-secret

server:
  port: 6004


#hystrix:
#  command:
#    getCommentsKey:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 3000
feign:
  hystrix:
    enabled: true
